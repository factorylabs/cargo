#!/usr/bin/env ruby
require "#{File.dirname(__FILE__)}/../lib/cargo/command_helpers.rb"

branch = git_branch
`git checkout master`
`git merge #{branch}`
`git push origin master`
`git branch -d #{branch}`
