#!/usr/bin/env ruby
require "#{File.dirname(__FILE__)}/../lib/cargo/command_helpers.rb"

branch = git_branch
`git checkout master`
`git pull origin master`
`git checkout #{branch}`
`git rebase master`
